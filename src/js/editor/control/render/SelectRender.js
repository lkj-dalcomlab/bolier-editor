import {ControlRender} from "../ControlRender.js";

export class SelectRender extends ControlRender {
    constructor(control) {
        super();
        this.control = control;
    }

    render(painter) {
        super.render(painter);
    }
}